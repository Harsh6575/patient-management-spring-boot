server:
  port: 4004

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # =====================================
        # AUTH SERVICE ROUTES (Port: 4005)
        # =====================================
        ## REST CLIENT -> http://localhost:4004/auth/login
        ## API GATEWAY -> http://auth-service:4005/login
        ## AUTH SERVICE (SPRING BOOT) -> / (Controller) will return auth message
        - id: auth-service-route
          uri: http://auth-service:4005
          predicates:
            - Path=/auth/**
          filters:
            - StripPrefix=1 # Remove /auth prefix before forwarding


        # =====================================
        # PATIENT SERVICE ROUTES (Port: 4000)
        # =====================================
        ## REST CLIENT -> http://localhost:4004/api/patients
        ## API GATEWAY -> http://patient-service:4000/patients
        ## PATIENT SERVICE (SPRING BOOT) -> /patients (Controller) will return patients array
        - id: patient-service-route
          uri: http://patient-service:4000
          predicates:
            - Path=/api/patients/**
          filters:
            - StripPrefix=1 # Remove /api prefix before forwarding
            - JwtValidation # Custom filter to validate JWT token
            # Important: here JwtValidation  written because spring will automatically search for a bean with this name + GatewayFilterFactory suffix,
            # and we have created a Component with JwtValidationGatewayFilterFactory name in the project

        ## OpenAPI docs for patient service
        - id: patient-service-docs
          uri: http://patient-service:4000
          predicates:
            - Path=/api-docs/patients
          filters:
            - RewritePath=/api-docs/patients, /v3/api-docs